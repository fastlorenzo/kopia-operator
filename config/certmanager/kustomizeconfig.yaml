# This configuration is for teaching kustomize how to update name and namespace reference in Certificate CR
nameReference:
  - kind: Issuer
    group: cert-manager.io
    fieldSpecs:
      - kind: Certificate
        group: cert-manager.io
        path: spec/issuerRef/name
